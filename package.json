{
  "name": "currency-keeper",
  "version": "1.0.0",
  "description": "A Node.js project that uses Express to serve a website with two functionalities: check currency rate at a given date and graph the change in a rate over a given period.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "marifdemirtas",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "ejs": "^2.6.2",
    "express": "^4.17.1",
    "request": "^2.88.0"
  }
}
